logger:
  level: debug
  format: text

server:
  host: "0.0.0.0"
  port: "8080"
  number_rooms: 3
  number_clients: 5
  number_messages: 30
  shutdown_timeout: "20s"

postgres:
  host: "postgres"
  port: "5432"
  ssl_mode: "disable"